# Seva Messenger - Инструкция по быстрому запуску

## Шаг 1: Установи Python и http.server (если нет)
```bash
# На любом компьютере с Python
cd /workspace/seva-messenger
python3 -m http.server 3000
```

## Шаг 2: Получи публичный адрес через ngrok

**Вариант А - Установка ngrok:**
1. Скачай ngrok с https://ngrok.com/
2. Установи и зарегистрируйся
3. Запусти:
```bash
ngrok http 3000
```
4. Получишь ссылку типа `https://abc123.ngrok.io` - её можно открывать на любом устройстве!

**Вариант Б - Cloudflare Quick Tunnel:**
1. Установи Cloudflare: `npm install -g cloudflared`
2. Запусти: `cloudflared tunnel --url localhost:3000`
3. Получишь публичную ссылку

## Шаг 3: Деплой на Railway (рекомендуется)

```bash
# Установка Railway CLI
npm install -g @railway/cli

# Авторизация
railway login

# Деплой
cd /workspace/seva-messenger
railway init
railway up
railway open
```

После этого получишь постоянную ссылку вида `https://seva-messenger.railway.app`

## Файлы проекта

📁 `/workspace/seva-messenger/`
├── 📄 server.js (Node.js сервер)
├── 📄 package.json (зависимости)
├── 📁 public/ (интерфейс)
│   ├── index.html
│   ├── styles.css
│   ├── app.js
│   ├── auth.js
│   └── zvonky.mp3
└── 📄 DEPLOY.md (подробная инструкция)

## Запуск

```bash
cd /workspace/seva-messenger
npm install
npm start
# Открой http://localhost:3000
```

## После запуска

1. Зарегистрируй 2-х пользователей в разных браузерах
2. Создай чат между ними
3. Нажми кнопку телефона для звонка
4. Прими звонок на втором устройстве
5. Разговаривайте!

Публичную ссылку получай через ngrok или задеплой на Railway! 🚀
